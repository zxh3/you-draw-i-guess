const GAME_STATE = {
  WAIT: "WAIT",
  START: "START",
};

const ROUND_TIME = 3; // in seconds

const NUM_ROUNDS = 2;

const GAME_STATUS_UPDATE_INTERVAL = 1; // in seconds

module.exports = {
  ROUND_TIME,
  GAME_STATE,
  NUM_ROUNDS,
  GAME_STATUS_UPDATE_INTERVAL,
};
